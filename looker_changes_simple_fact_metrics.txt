measure: 150_vehicle_sales {
    label: vehicle_sales
    type: number
    value_format_name: decimal_0
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.vehicle_sales_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.vehicle_sales_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.vehicle_sales_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.vehicle_sales_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.vehicle_sales_entity)
        END ;;
}

measure: 151_mechanical_sales {
    label: mechanical_sales
    type: number
    value_format_name: decimal_0
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.mechanical_sales_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.mechanical_sales_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.mechanical_sales_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.mechanical_sales_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.mechanical_sales_entity)
        END ;;
}

measure: 152_maintenance_sales {
    label: maintenance_sales
    type: number
    value_format_name: decimal_0
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.maintenance_sales_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.maintenance_sales_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.maintenance_sales_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.maintenance_sales_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.maintenance_sales_entity)
        END ;;
}

measure: 153_cpo_sales {
    label: cpo_sales
    type: number
    value_format_name: decimal_0
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.cpo_sales_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.cpo_sales_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.cpo_sales_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.cpo_sales_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.cpo_sales_entity)
        END ;;
}

measure: 154_total_sales_security {
    label: total_sales_security
    type: number
    value_format_name: decimal_0
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.total_sales_security_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.total_sales_security_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.total_sales_security_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.total_sales_security_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.total_sales_security_entity)
        END ;;
}

measure: 155_mechanical_penetration_pct {
    label: mechanical_penetration_pct
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.mechanical_penetration_pct_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.mechanical_penetration_pct_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.mechanical_penetration_pct_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.mechanical_penetration_pct_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.mechanical_penetration_pct_entity)
        END ;;
}

measure: 156_maintenance_penetration_pct {
    label: maintenance_penetration_pct
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.maintenance_penetration_pct_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.maintenance_penetration_pct_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.maintenance_penetration_pct_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.maintenance_penetration_pct_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.maintenance_penetration_pct_entity)
        END ;;
}

measure: 157_cpo_penetration_pct {
    label: cpo_penetration_pct
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.cpo_penetration_pct_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.cpo_penetration_pct_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.cpo_penetration_pct_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.cpo_penetration_pct_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.cpo_penetration_pct_entity)
        END ;;
}

measure: 199_mech_cpo_pen_pct {
    label: mech_cpo_pen_pct
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.mech_cpo_pen_pct_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.mech_cpo_pen_pct_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.mech_cpo_pen_pct_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.mech_cpo_pen_pct_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.mech_cpo_pen_pct_entity)
        END ;;
}

measure: 200_mech_cpo_pen_pct_obj {
    label: mech_cpo_pen_pct_obj
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.mech_cpo_pen_pct_obj_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.mech_cpo_pen_pct_obj_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.mech_cpo_pen_pct_obj_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.mech_cpo_pen_pct_obj_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.mech_cpo_pen_pct_obj_entity)
        END ;;
}

measure: 201_saf_new_accruals {
    label: saf_new_accruals
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_new_accruals_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_new_accruals_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_new_accruals_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_new_accruals_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_new_accruals_entity)
        END ;;
}

measure: 202_saf_new_expired {
    label: saf_new_expired
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_new_expired_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_new_expired_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_new_expired_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_new_expired_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_new_expired_entity)
        END ;;
}

measure: 203_saf_new_utilization {
    label: saf_new_utilization
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_new_utilization_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_new_utilization_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_new_utilization_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_new_utilization_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_new_utilization_entity)
        END ;;
}

measure: 204_saf_ps_accruals {
    label: saf_ps_accruals
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_ps_accruals_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_ps_accruals_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_ps_accruals_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_ps_accruals_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_ps_accruals_entity)
        END ;;
}

measure: 205_saf_ps_expired {
    label: saf_ps_expired
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_ps_expired_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_ps_expired_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_ps_expired_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_ps_expired_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_ps_expired_entity)
        END ;;
}

measure: 206_saf_ps_utilization {
    label: saf_ps_utilization
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_ps_utilization_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_ps_utilization_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_ps_utilization_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_ps_utilization_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_ps_utilization_entity)
        END ;;
}

measure: 207_saf_flex_accruals {
    label: saf_flex_accruals
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_flex_accruals_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_flex_accruals_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_flex_accruals_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_flex_accruals_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_flex_accruals_entity)
        END ;;
}

measure: 208_saf_flex_expired {
    label: saf_flex_expired
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_flex_expired_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_flex_expired_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_flex_expired_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_flex_expired_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_flex_expired_entity)
        END ;;
}

measure: 209_saf_flex_utilization {
    label: saf_flex_utilization
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_flex_utilization_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_flex_utilization_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_flex_utilization_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_flex_utilization_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_flex_utilization_entity)
        END ;;
}

measure: 210_saf_total_accruals {
    label: saf_total_accruals
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_total_accruals_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_total_accruals_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_total_accruals_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_total_accruals_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_total_accruals_entity)
        END ;;
}

measure: 211_saf_total_expired {
    label: saf_total_expired
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_total_expired_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_total_expired_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_total_expired_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_total_expired_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_total_expired_entity)
        END ;;
}

measure: 212_saf_total_utilization {
    label: saf_total_utilization
    type: number
    value_format_name: percent_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_total_utilization_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_total_utilization_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_total_utilization_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_total_utilization_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_total_utilization_entity)
        END ;;
}

measure: 213_saf_care_connect_spend {
    label: saf_care_connect_spend
    type: number
    value_format_name: usd_2
    sql: CASE
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Division'
            THEN max(${TABLE}.saf_care_connect_spend_division)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Region'
            THEN max(${TABLE}.saf_care_connect_spend_region)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Zone'
            THEN max(${TABLE}.saf_care_connect_spend_zone)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'District'
            THEN max(${TABLE}.saf_care_connect_spend_district)
          WHEN ${simple_dim_entity.hierarchy_level_name} = 'Entity'
            THEN max(${TABLE}.saf_care_connect_spend_entity)
        END ;;
}
